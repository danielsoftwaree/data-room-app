import { DataroomsScreen } from '../features/datarooms';

/**
 * App shell. Once a router is added, route-to-feature wiring lives here;
 * features themselves stay self-contained under src/features/*.
 */
export function App() {
  return <DataroomsScreen />;
}
