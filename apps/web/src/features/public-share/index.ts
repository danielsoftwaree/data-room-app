/**
 * Public entry of the public-share feature: the unauthenticated `/share/:slug`
 * page where an external viewer unlocks and downloads a shared file.
 */
export { PublicShareScreen } from './components/public-share-screen';
